apiVersion: v1
kind: Service
metadata:
  name: {{ include "qcon-guide.fullname" . }}
  labels:
    {{- include "qcon-guide.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qcon-guide.selectorLabels" . | nindent 4 }}
